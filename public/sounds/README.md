# UI sounds

Place **`card_button.mp3`** in this folder. The design playground loads it from `/sounds/card_button.mp3` for:

- **Radio & segmented** play area — option / variant clicks  
- **Outline + under-glow** tab strip — Drive / Climate / Media / System when that variant is selected
