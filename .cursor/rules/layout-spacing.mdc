---
description: Page layout spacing rules for main content area relative to the avatar bar, top bar, and sidebar. Apply whenever building or modifying page layouts, content areas, or any component placed inside the main content region.
globs:
  - src/app/**/*.tsx
  - src/components/**/*.tsx
---

# Layout Spacing Standards

These are the canonical spacing values for the app shell. Every page and content component must respect them.

## Reference Anatomy

```
┌──────────────────────────────────────────────────┐
│  Sidebar (left)  │  Avatar bar (64 px / h-16)    │
│                  ├───────────────────────────────-│
│                  │← 32 px padding-left (px-8)    →│
│                  │                                │
│                  │  ↕ 24 px gap below top bar     │
│                  │                                │
│                  │  Top bar (TopBar) / Content    │
│                  │                                │
└──────────────────┴───────────────────────────────-┘
```

## Spacing Values

| Relationship                              | Value    | Tailwind class | Notes                                  |
| ----------------------------------------- | -------- | -------------- | -------------------------------------- |
| Below top bar → first content element     | **24 px** | `pt-6`         | Applied on the content wrapper div     |
| Left edge (from sidebar) → content        | **32 px** | `px-8`         | Horizontal padding on Avatar bar, Top bar & content |
| Avatar bar height                         | **64 px** | `h-16`         | Fixed; do not change                   |

## Implementation Rules

1. **Below the avatar bar**: The inner `<div>` inside `<main>` that wraps the page `<TopBar />` should use `pt-6` (24 px) top padding so there is consistent space between the avatar bar and the top bar (see `src/app/page.tsx`).

   **Main column line field:** `AppMainLineFieldPattern` paints behind **all** routes in `<main>` (see `page.tsx`). Pages use the normal **`pt-6`** wrapper before `TopBar` (same as Staff / Customers); the pattern shows through the padding.

2. **From the sidebar (left padding)**: All content inside the main column uses `px-8` (32 px) horizontal padding. This applies to `<AvatarBar>`, the page-level `<TopBar>`, and `<main>` content containers.

3. **Do not nest these paddings.** The `TopBar` component already carries `px-8`. Child content sections that sit inside `<main>` should also use `px-8` individually (not inherit it from a parent wrapper that double-pads).

## Current Implementation Reference

Matches `src/app/page.tsx` (inside `AppGroovedMainColumn`):

```tsx
<AvatarBar />
<main className="flex flex-1 flex-col overflow-hidden">
  <div className="flex flex-1 flex-col min-h-0 overflow-hidden pt-6">
    <TopBar breadcrumbs={…} title={…} right={…} />
    {/* …page body; use px-8 where needed */}
  </div>
</main>
```

`pt-6` is the 24px gap **below the avatar bar** and **above** the page `TopBar`.

## Quick Checklist

- [ ] Gap between the avatar bar and the page `TopBar` is exactly 24 px (`pt-6` on the page wrapper).
- [ ] Horizontal padding from sidebar edge is exactly 32 px (`px-8`).
- [ ] Avatar bar height remains 64 px (`h-16`).
- [ ] New page components do not introduce extra top/left padding that compounds with these values.
